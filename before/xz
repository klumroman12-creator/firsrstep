from 
def encrypt_by_key(message, key):
    alpa = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    cipher = ""
    message = message.upper()

    for letter in message:
        if letter in alpa:
            index = (alpa.find(letter) + key) % len(alpa)
            cipher += alpa[index]
        else:
            cipher += letter

    print(cipher)

encrypt_by_key("To be or not to be", 5)
encrypt_by_key("Brutte is killer", 3)

